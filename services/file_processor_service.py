from typing import Any, Dict, List, Optional

from .base_agent import BaseAgent  # relative import from services (we'll move base_agent)

# ... copy content? Wait base_agent is still in agents. So we keep import from agents.base_agent.